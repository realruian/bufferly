# PastePop PRD

版本：v0.2  
日期：2026-07-07  
状态：Draft  
负责人：Tian  
产品类型：macOS 原生桌面应用  

> v0.2 变更：产品定位从「开发者和 AI heavy user 的剪贴板工作台」调整为「普通 macOS 用户的本地优先剪贴板助手」。开发者向能力降级为未来高级功能。

## 1. 产品概述

PastePop 是一款面向普通 macOS 用户的本地优先剪贴板助手。

它围绕每个人每天都在复制的文字、链接、图片、文件，提供轻松找回、快速搜索、安全粘贴的能力。所有内容默认只保存在本机，不上传；密码、验证码等敏感内容自动保护。

一句话定位：

> 普通 macOS 用户的本地优先剪贴板助手：复制过的东西，随时找得回来。

## 2. 背景与问题

每个人每天都会频繁复制大量临时信息：

- 短信 / 邮件里的验证码
- 网页链接
- 收货地址、电话、邮箱
- 聊天记录片段、临时文字
- 截图和图片
- 文件

系统剪贴板只保存当前一条内容，刚复制的验证码很容易被下一次复制顶掉；通用剪贴板工具要么依赖云同步和订阅，要么缺少对敏感内容的自动保护，要么操作复杂、更像专业工具。

PastePop 要解决的问题：

1. 复制过的有用内容经常找不回来。
2. 验证码、地址、链接这类高频内容需要反复复制粘贴。
3. 剪贴板历史可能泄露密码、验证码等敏感内容。
4. 用户担心剪贴板内容被上传到云端。
5. 用户需要一个低打扰、像系统功能一样自然的助手，而不是一个需要学习的重型工具。

## 3. 目标用户

### 3.1 核心用户

- 日常使用 macOS 的普通用户
- 经常在浏览器、聊天工具、邮件、文档之间复制内容的人
- 关心隐私、不希望剪贴板内容上传云端的人

### 3.2 次级用户

- 产品经理、设计师、运营、客服等高频复用文本片段的人
- 开发者等进阶用户（未来通过高级功能满足，不影响主线体验）

## 4. 用户场景

### 场景 1：找回被顶掉的验证码

用户刚复制了短信验证码，随手又复制了别的内容导致验证码丢失。用户按快捷键呼出 PastePop，验证码就在最前面，回车粘贴。

### 场景 2：复用常用内容

用户把常用的收货地址、发票抬头、回复话术固定（pin）起来。需要时按快捷键呼出，输入关键词，直接粘贴。

### 场景 3：找回昨天复制的链接

用户昨天复制过一个网页链接，今天想再发给朋友。呼出 PastePop，搜索记得的关键词，链接自动归类为「链接」类型，一眼找到。

### 场景 4：找回复制过的图片和文件

用户复制过一张截图或一个文件，后来又复制了别的内容。呼出 PastePop，图片有缩略图、文件有图标和名称，选中即可原样粘贴。

### 场景 5：敏感内容自动保护

用户复制密码、验证码或密钥。PastePop 判断为敏感内容，默认不保存明文，或只保存脱敏预览，不会在历史里泄露。

## 5. 产品目标

### 5.1 MVP 目标

在 macOS 上提供一个稳定、快速、低打扰的本地剪贴板历史助手，让普通用户不用学习就能找回复制过的内容，并默认保护隐私。

### 5.2 成功指标

MVP 阶段先以个人使用和可验证行为为指标：

| 指标 | 目标 |
|---|---|
| 呼出速度 | 快捷键触发后 200ms 内显示窗口 |
| 搜索响应 | 500 条历史内搜索无明显卡顿 |
| 剪贴板捕获准确性 | 普通文本复制可稳定入库 |
| 敏感内容保护 | 明确规则命中的密码 / 验证码 / 密钥默认不保存明文 |
| 日常可用性 | 作者连续使用 7 天，不需要切回其他剪贴板工具 |
| 崩溃率 | 日常使用不出现可复现崩溃 |

## 6. MVP 范围

### 6.1 必须有

1. 剪贴板监听
   - 监听 macOS 系统剪贴板文本变化。
   - 自动保存新文本内容。
   - 相同内容去重。

2. 历史记录
   - 保存最近 500 条文本历史。
   - 显示内容预览、复制时间、类型标识。
   - 支持删除单条记录。

3. 快捷键呼出
   - 支持全局快捷键呼出/隐藏主面板。
   - 默认快捷键建议：`Option + Space`，后续允许配置。

4. 搜索
   - 支持按内容模糊搜索。
   - 搜索结果实时更新。
   - 支持键盘上下选择。

5. 粘贴
   - 回车将选中内容写回剪贴板。
   - 尽量自动粘贴到当前前台 App。
   - 如自动粘贴受权限限制，至少保证内容已复制到剪贴板。

6. Pin 常用内容
   - 用户可以 pin 常用内容，例如地址、话术、链接。
   - Pin 内容固定展示在独立区域或搜索结果顶部。

7. 类型识别（面向普通用户）
   - 链接（URL）
   - 邮箱
   - 验证码
   - 电话
   - 地址
   - 图片 / 文件
   - 普通文本

8. 敏感内容保护
   - 检测疑似密码、验证码、密钥、`key=value` 配置片段。
   - 默认不保存明确敏感内容明文。
   - UI 中显示“已保护敏感内容”的占位记录或完全跳过，具体策略开发时确认。

9. 本地存储
   - 历史记录默认只保存在本机。
   - 不做云同步。

### 6.2 应该有

1. 粘贴为纯文本。
2. 长文本自动生成短标题。
3. 设置页：历史保留数量、敏感保护开关、快捷键设置。

### 6.3 可以后做（高级功能，不作为主线卖点）

1. 图片剪贴板历史。
2. 文件剪贴板历史。
3. 片段库与片段展开。
4. 多段复制栈。
5. 按类型的快捷动作（打开链接、拨打电话、写邮件等）。
6. 开发者向类型识别与工具（代码、格式化等）。
7. AI 总结 / 改写 / 翻译（本地优先、显式开启）。
8. iCloud 同步。
9. Raycast / Alfred 集成。

## 7. 不做范围

MVP 明确不做：

- Windows / Linux 客户端
- iPhone / iPad 客户端
- 云同步
- 团队共享
- 账号系统
- 订阅付费
- 浏览器插件
- 完整笔记系统
- 所有文件类型的剪贴板管理
- 开发者向工具作为主线功能（代码高亮、格式化、命令执行等）
- 复杂 AI 功能

## 8. 核心交互

### 8.1 主流程

1. 用户复制内容。
2. PastePop 后台监听剪贴板变化。
3. 通过敏感规则检查。
4. 合格内容写入本地历史。
5. 用户按全局快捷键呼出面板。
6. 用户搜索或键盘选择条目。
7. 用户回车粘贴。
8. 面板自动隐藏。

### 8.2 搜索面板

面板应满足：

- 低打扰，浮层展示。
- 默认聚焦搜索框。
- 支持键盘优先操作。
- 不把 UI 做成重型素材库。
- 列表信息密度高，但要能快速扫读。

建议布局：

```text
------------------------------------------------+
| Search clips...                               |
+------------------------------------------------+
| Pinned                                         |
| [地址] 上海市徐汇区...                     pin |
| [文本] 常用回复话术...                     pin |
+------------------------------------------------+
| Recent                                         |
| [验证码] 384729                            now |
| [链接] apple.com/...                       2m  |
| [图片] 屏幕截图 2026-07-07...              5m  |
| [文本] 会议纪要片段...                     8m  |
+------------------------------------------------+
```

## 9. 功能需求

### 9.1 剪贴板监听

| 编号 | 需求 | 优先级 | 验收标准 |
|---|---|---|---|
| FR-001 | App 启动后监听文本剪贴板变化 | P0 | 复制普通文本后，历史列表出现新记录 |
| FR-002 | 相同内容不重复保存 | P0 | 连续复制同一文本，只保留一条或更新时间 |
| FR-003 | 忽略空文本和纯空白文本 | P0 | 复制空白内容不会新增记录 |

### 9.2 历史记录

| 编号 | 需求 | 优先级 | 验收标准 |
|---|---|---|---|
| FR-010 | 展示最近剪贴板历史 | P0 | 面板中能看到最近复制内容 |
| FR-011 | 支持删除单条历史 | P1 | 删除后该条不再出现在列表 |
| FR-012 | 限制历史数量 | P0 | 超过 500 条后自动淘汰最旧记录 |

### 9.3 搜索与选择

| 编号 | 需求 | 优先级 | 验收标准 |
|---|---|---|---|
| FR-020 | 支持模糊搜索 | P0 | 输入关键词后列表只显示匹配项 |
| FR-021 | 支持键盘上下移动 | P0 | 上下键能改变当前选中项 |
| FR-022 | 支持回车选择 | P0 | 回车后选中内容写入剪贴板 |

### 9.4 Pin

| 编号 | 需求 | 优先级 | 验收标准 |
|---|---|---|---|
| FR-030 | 用户可 pin 历史条目 | P0 | 被 pin 的条目显示在 pinned 区域 |
| FR-031 | 用户可 unpin | P0 | 取消后条目回到普通历史 |

### 9.5 类型识别

| 编号 | 需求 | 优先级 | 验收标准 |
|---|---|---|---|
| FR-040 | 识别链接（URL） | P0 | 链接显示「链接」类型标识 |
| FR-041 | 识别验证码 | P0 | 短信 / 邮件验证码显示「验证码」类型标识 |
| FR-042 | 识别邮箱 | P1 | 邮箱地址显示「邮箱」类型标识 |
| FR-043 | 识别电话 | P1 | 电话号码显示「电话」类型标识 |
| FR-044 | 识别地址 | P2 | 常见收货地址显示「地址」类型标识 |
| FR-045 | 识别图片 / 文件 | P0 | 图片显示缩略图、文件显示图标与名称 |

> 技术内容（代码、JSON、命令等）的识别保留为未来高级功能，不在主线验收范围内。

### 9.6 敏感内容保护

| 编号 | 需求 | 优先级 | 验收标准 |
|---|---|---|---|
| FR-050 | 识别 `key=value` 风格密钥配置 | P0 | `API_KEY=...` 不保存明文 |
| FR-051 | 识别 password/token/secret 关键词 | P0 | 命中关键词的内容默认不保存明文 |
| FR-052 | 识别常见密钥 / 令牌形态 | P1 | 明显密钥字符串不保存明文 |
| FR-053 | 用户可在设置里查看保护策略 | P2 | 设置页能看到规则说明 |

### 9.7 粘贴选项

| 编号 | 需求 | 优先级 | 验收标准 |
|---|---|---|---|
| FR-060 | 粘贴为纯文本 | P1 | 富文本复制后可输出纯文本 |

> 面向开发者的格式转换（URL 参数清理、JSON 格式化 / 压缩等）保留为未来高级功能。

## 10. 非功能需求

| 类别 | 要求 |
|---|---|
| 性能 | 快捷键呼出到可交互目标 200ms 内 |
| 存储 | 本地保存，默认不联网 |
| 隐私 | 敏感内容默认不保存明文 |
| 稳定性 | 后台监听不应导致明显 CPU 占用 |
| 可访问性 | 搜索、选择、粘贴主流程必须可纯键盘完成 |
| 可靠性 | App 重启后历史和 pinned 内容仍存在 |
| 可维护性 | 剪贴板监听、存储、过滤、识别、UI 分层实现 |

## 11. 技术方向

当前确认采用 macOS 原生技术栈：

- Swift
- SwiftUI
- AppKit
- SQLite
- Keychain

职责划分建议：

| 模块 | 技术 | 说明 |
|---|---|---|
| 主 UI | SwiftUI | 搜索面板、历史列表、设置页 |
| 系统能力 | AppKit | 剪贴板监听、全局快捷键、窗口层级、菜单栏 |
| 本地数据库 | SQLite | 保存历史、pin、设置 |
| 安全存储 | Keychain | 保存加密密钥或敏感配置 |
| 内容识别 | Swift service | 链接 / 邮箱 / 验证码 / 电话 / 地址 / 图片 / 文件类型判断 |
| 敏感保护 | Swift service | 密码 / 验证码 / 密钥 / `key=value` 规则检测 |

## 12. 数据模型草案

### ClipItem

| 字段 | 类型 | 说明 |
|---|---|---|
| id | UUID | 主键 |
| content | String | 剪贴板文本内容，敏感内容不保存明文 |
| preview | String | UI 预览文本 |
| contentHash | String | 去重用 hash |
| type | String | url / email / otp / phone / address / image / file / text（代码等技术类型保留为高级能力） |
| isPinned | Bool | 是否 pin |
| isSensitive | Bool | 是否命中敏感规则 |
| createdAt | Date | 首次复制时间 |
| updatedAt | Date | 最近复制时间 |
| sourceApp | String? | 来源 App，后续可选 |

### AppSettings

| 字段 | 类型 | 默认值 |
|---|---|---|
| maxHistoryCount | Int | 500 |
| globalShortcut | String | Option+Space |
| enableSensitiveFilter | Bool | true |
| autoPasteAfterSelect | Bool | true |
| launchAtLogin | Bool | false |

## 13. 里程碑

### M0：项目骨架

- 初始化 Swift macOS app。
- 建立模块目录。
- App 可启动。
- 有基础主窗口。

### M1：剪贴板历史

- 后台监听文本剪贴板。
- 内存中展示历史。
- 去重。
- 搜索。

### M2：可用工作流

- 全局快捷键呼出。
- 回车复制/粘贴。
- Pin。
- 本地持久化。

### M3：类型识别与隐私保护

- 面向普通用户的类型识别（链接、邮箱、验证码等）。
- 敏感内容保护。

### M4：自用打磨

- 设置页。
- 启动项。
- 菜单栏图标。
- 错误处理。
- 连续 7 天自用修复。

## 14. 验收标准

MVP 可认为完成，当满足：

1. App 能在 macOS 正常启动。
2. 复制普通文本后，PastePop 能捕获并显示。
3. 按全局快捷键可呼出搜索面板。
4. 搜索、上下选择、回车复制/粘贴流程可用。
5. Pin / unpin 可用。
6. App 重启后历史记录仍存在。
7. 明确敏感内容不会保存明文。
8. 作者能连续使用 7 天，不需要退回其他剪贴板工具完成日常工作。

## 15. 开放问题

1. 默认快捷键是否使用 `Option + Space`？是否会和现有工具冲突？
2. 敏感内容命中后，是完全跳过，还是保存脱敏占位记录？
3. 自动粘贴是否需要请求辅助功能权限？第一版是否接受只复制不自动粘贴？
4. 数据库是否需要加密？如果加密，是否使用 Keychain 保存密钥？
5. 第一版是否支持图片？当前建议不支持。
6. UI 是做底部横向卡片流，还是 Raycast 风格垂直列表？
7. 是否需要显示来源 App？

## 16. 参考资料

- Atlassian PRD 模板：强调 PRD 应定义产品目的、功能和需求，并包含目标、成功指标、假设、用户故事、设计和开放问题。https://www.atlassian.com/software/confluence/templates/product-requirements
- Atlassian PRD 指南：强调 PRD 是跨职能团队的单一事实源，敏捷 PRD 应保持足够上下文和可更新性。https://www.atlassian.com/agile/requirements
- Aha PRD 模板指南：强调 PRD 应包含目标、范围、特性、发布和清晰可执行的需求。https://www.aha.io/roadmapping/guide/requirements-management/what-is-a-good-product-requirements-document-template
- Cavaro PRD 模板：强调需求应可测试，避免“系统应该很快”这类模糊表述，改为明确性能目标。https://www.cavaro.io/templates/product-requirements-document
- mdkit PRD 写作指南：强调写给工程师能读懂的 PRD，保持结构清晰、避免反模式，并可用 Markdown 维护。https://mdkit.io/blog/how-to-write-prd
