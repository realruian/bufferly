# PastePop 迭代路线图

> 本文件是产品迭代计划。设计规范见 [`DESIGN.md`](DESIGN.md)，产品范围见 [`CLAUDE.md`](CLAUDE.md)。
> 最近更新：2026-08-13。

## 战略判断

PastePop 定位「普通 macOS 用户的本地优先剪贴板助手」。

- **普通用户的真实痛点很朴素**：刚复制的验证码被新内容顶掉、想找回昨天复制的地址或链接、截图和文件复制完就丢。现有工具要么面向重度用户（Raycast、Pastebot），要么依赖订阅和云同步（Paste），要么功能极简（Maccy）。
- **信任是第一卖点**：剪贴板里有密码、验证码、隐私聊天记录。PastePop 的差异化是**默认本地保存、不上传任何内容、敏感内容自动保护**，并且在设置页把这些讲清楚。
- **体验要像系统功能**：原生 macOS 26 界面、低打扰、快捷键一呼即出、回车即贴。用户不需要学习，也不需要配置。

**结论**：主线是把「找回复制过的东西」这件事做到简单、可靠、可信。开发者 / AI 工作流相关能力（MCP、AI 上下文打包等）保留为未来高级功能，不作为主线卖点。

## 阶段规划

### 阶段 0 · 功能补齐（已完成 ✅）

把剪贴板能力补到日常自用无短板。

- [x] **图片剪贴板**：监听 / 入库 / 卡片缩略图预览 / 回写粘贴。
- [x] **文件剪贴板**：Finder 复制的文件，显示文件图标 + 名称，回写粘贴。
- [x] **富文本（RTF）**：保留富文本格式，卡片按富文本渲染，回写保留格式；纯文本作兜底。
- [x] 二进制 / 大附件不进 SQLite 主库，存到 `Application Support/PastePop/blobs/`，DB 只存引用。
- [x] 去重适配：图片按 SHA-256 内容哈希去重、文件按路径去重；命中去重不写孤儿 blob。
- [x] 卡片显示来源 App 图标（记录来源 bundle id，按需解析图标）。
- [x] 富链接预览（LinkPresentation）：**默认关闭**，开启后才联网获取 URL 标题与图标，守住隐私优先。
- [x] **组合筛选**：按类型、来源 App、今天 / 最近 7 天 / 最近 30 天组合筛选。
- [x] **暂停记录**：菜单栏可暂停 15 分钟、1 小时或直到手动恢复，到期自动恢复。
- [x] **固定内容整理**：固定内容支持自定义名称和单层分组；旧条目自动归入「未分组」。

### 阶段 1 · 普通用户体验

- [ ] **贴近日常的类型识别**：验证码、地址、电话、邮箱、链接、图片、文件等普通用户能理解的类型；弱化「代码 / JSON / 命令」等技术类型的展示优先级。
- [ ] **验证码快速取用**：识别短信 / 邮件验证码，呼出面板即置顶可贴。
- [ ] **零配置开箱即用**：首次启动引导只讲三件事——怎么呼出、内容只存在本机、敏感内容自动保护。
- [ ] **数据库静态加密**：SQLite + AES，密钥进 Keychain；敏感占位也加密。

### 阶段 2 · 可靠与安心

- [ ] 隐私仪表盘强化：一眼看清记录了什么、保存多久、哪些 App 不记录。
- [x] 历史整理：按类型 / 来源 App / 时间快速筛选，找回更省心。
- [ ] 安装与更新体验：公证分发、平滑升级、崩溃自愈。

### 阶段 3 · 高级功能（可选，不影响主线）

面向进阶用户的能力，默认关闭或按需开启，不作为主线卖点：

- [ ] 片段展开（输入触发词自动展开常用内容）。
- [ ] 按类型的快捷动作（打开链接、拨打电话、写邮件等）。
- [ ] 开发者 / AI 工作流集成（如本地 MCP 接口、上下文打包）：本地运行、用户显式授权、可随时撤销。

## 竞品参考

| 产品 | 定位 | 占住的点 |
|---|---|---|
| [Paste](https://pasteapp.io/) | 订阅、精致 UI + iCloud 同步 | 颜值、跨设备、分发 |
| [Maccy](https://github.com/p0deje/Maccy) | 免费开源、纯本地 | 隐私基线 |
| Raycast | 启动器顺带剪贴板 | 扩展生态 |
| Pastebot | 买断制、老牌 | 稳定口碑 |

PastePop 的位置：像 Paste 一样好看好用，像 Maccy 一样本地免费可信，且默认帮普通用户保护敏感内容。

## 暂不做（守住定位）

- 云同步、团队共享、移动端、复杂订阅系统、全格式支持。
- 默认把内容外发给任何云端服务（一切联网能力显式开启才生效）。
- 把开发者向功能（代码高亮、JSON 工具、命令执行、AI agent 集成）做成主线卖点。
